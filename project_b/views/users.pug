extends layout 

block content
    h1 Users 
    each user in users
        p 
            | First name: #{user.firstname} <br />
            | Last name: #{user.lastname} <br />
            | Email: #{user.email} <br />
            | Password: #{user.password} 